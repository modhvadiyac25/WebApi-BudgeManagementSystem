@model WebApi_BudgeManagementSystem.ViewModels.SelectIncomeCategoryViewModel

@{
    ViewBag.Title = "AddIncome";
}

<h2>AddIncome</h2>

@using (Html.BeginForm("AddIncome", "Consume", FormMethod.Post))
{
    @Html.AntiForgeryToken()

<div class="form-horizontal">

    <hr />
    @Html.ValidationSummary(true, "", new { @class = "text-danger" })

    @*<div class="form-group">
            <label asp-for="inc_cat" class="control-label">Income Category</label>

            <select style="height:auto" class="dropdown form-control" asp-for="inc_cat">
                <option value="" selected disabled> Select Category</option>
                @foreach (var item in Model.categories)
                {
                    <option value="@item.ToString()">@item.ToString()</option>
                }
            </select>
        </div>*@
    @Html.DropDownListFor(model => model.inc_cat, new SelectList(Model.categories) ,"Select Category", new { @class = "form-control" })

    @*@Html.DropDownList(model => model.categories, new SelectList(type(string)), "Select Category", new { @class = "form-control" })*@

    <div class="form-group">
        
        <div class="col-md-10">
            @Html.LabelFor(model => model.inc_amount,"Add Amount", new { htmlAttributes = new { @class = "form-control" } })
            @Html.EditorFor(model => model.inc_amount, new { htmlAttributes = new { @class = "form-control" } })
            @Html.ValidationMessageFor(model => model.inc_amount, "", new { @class = "text-danger" })
        </div>
    </div>
    @Html.HiddenFor(model => model.uid, new { Value = @Int32.Parse(HttpContext.Current.Session["uid"].ToString()) })

    @*<input type="hidden" asp-for="uid" value="">*@
    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Add Income" class="btn btn-default" />
        </div>
    </div>
</div>
}



@*<form asp-action="InsertIncome" class="form-group">

    <h1>Binding data into Dropdown list</h1>
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>

    <div class="form-group">
        <label asp-for="inc_cat" class="control-label">Income Category</label>
        <select style="height:auto" class="dropdown form-control" name="cat">
            <option value="" selected disabled> Select Category</option>
            @foreach (var item in Model.categories)
            {
                <option value="@item.ToString()">@item.ToString()</option>
            }
        </select>
    </div>

    <input type="hidden" name="uid" value="@Int32.Parse(HttpContext.Current.Session["uid"].ToString())">

    <div class="form-group">
        <label asp-for="inc_amount" class="control-label">Add Amount</label>
        <label asp-for="inc_amount" class="control-label"></label>
        <input name="inc_amount" class="form-control" />
        <span asp-validation-for="inc_amount" class="text-danger"></span>
    </div>
    <div class="form-group">
        <button type="submit" class="btn btn-primary"> Add Income </button>
    </div>
</form>*@

